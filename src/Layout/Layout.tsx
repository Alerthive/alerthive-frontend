import Head from "next/head";

const Layout = () => {
  return (
    <>
      <Head>
        <title>AlertHive</title>
        <meta name="description" content="Twitter Alert Dashboard" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
      </Head>
    </>
  );
};

export default Layout;
